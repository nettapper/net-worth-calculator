version: 2.1

jobs:
  build:
    docker:
      - image: node:current-alpine
    steps:
      - checkout
      - run: npm run build
  test-frontend:
    docker:
      - image: node:current-alpine
    steps:
      - checkout
      - run: npm run test
  test-server:
    docker:
      - image: node:current-alpine
    steps:
      - checkout
      - run: npm run test-server

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - build
      - test-frontend:
          requires:
            - build
      - test-server:
          requires:
            - build


