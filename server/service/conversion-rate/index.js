const axios = require('axios');

function conversionRate(currentCurrency, newCurrency) {
  return {
    rate: 1,
    info: ['b'],
    warning: [],
    error: []
  };
}

module.exports = conversionRate;
